// import { gzipSync } from "bun";

// export default function compressor(html: string) {
//   const enc = new TextEncoder();
//   const compressed = gzipSync(enc.encode(html));
//   return compressed;
// }
